import cv2
import numpy as np

class FacePreprocess:
    @staticmethod
    def normalize_image(image):
        # Resize and normalize
        return image

    @staticmethod
    def extract_region(image, landmarks, indices):
        # Extract region logic using numpy slicing
        return None
